# PitchIQ — Python dependencies
# Install with:  python -m pip install -r requirements.txt

# --- Core analysis / ML (Phases 1–3) ---
pandas>=2.2
numpy>=2.0
matplotlib>=3.8
seaborn>=0.13
scipy>=1.11
scikit-learn>=1.8,<1.9     # must load the saved model pickle (trained on 1.8.x)
certifi>=2023.0            # trusted HTTPS (download_data.py + player photos)

# --- Web app (Phase 4) ---
streamlit>=1.40            # st.pills top navigation
joblib>=1.3
plotly>=5.20
pyarrow>=15                # reads data/deliveries.parquet
